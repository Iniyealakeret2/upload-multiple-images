import { twoSums } from "./two_sums/two_sums";

// SOLUTION TO TWO SUMS ALGORITHM PROBLEM
// TIME COMPLEXITY IS O(N) AND SPACE-COMPLEXITY IS O(N)
const numbers: number[] = [2];
const target = 2;

console.log(twoSums(numbers, target));
